import React from "react"
import "./ParallaxImage.scss"

const ParallaxImageOne = ({ image }) => {
  return (
    <div className='parallax1'>
      <img sec={image} alt='' />
    </div>
  )
}

export default ParallaxImageOne
