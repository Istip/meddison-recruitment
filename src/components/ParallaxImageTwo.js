import React from "react"
import "./ParallaxImage.scss"

const ParallaxImageTwo = ({ image }) => {
  return (
    <div className='parallax2'>
      <img sec={image} alt='' />
    </div>
  )
}

export default ParallaxImageTwo
